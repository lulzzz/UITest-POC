CREATE TABLE [FormConfig].[DisplayValidation] (
    [Id]   INT            IDENTITY (1, 1) NOT NULL,
    [Name] NVARCHAR (MAX) NOT NULL,
    CONSTRAINT [PK_FormConfig.DisplayValidation] PRIMARY KEY CLUSTERED ([Id] ASC)
);

